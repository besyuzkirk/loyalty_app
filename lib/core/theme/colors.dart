import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Ana renk paleti
  static const primary = Color(0xFF4A90E2); // Mavi ton
  static const secondary = Color(0xFF50E3C2);
  static const accent = Color(0xFFFFC107);

  // 🌞 Light tema renkleri
  static const lightBackground = Colors.white;
  static const lightSurface = Color(0xFFF4F6F8);
  static const lightText = Colors.black87;

  // 🌙 Dark tema renkleri
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkText = Colors.white70;
}
